---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<h1>🚀 欢迎来到我的 Astro 博客</h1>
			<p>
				这是一个基于 <a href="https://astro.build/">Astro</a> 构建的全新博客，拥有完全的自定义自由度！
				你可以添加任何你想要的功能：背景图片、音效、动画、交互效果等等。
			</p>

			<h2>✨ 功能特性</h2>
			<ul>
				<li>🎨 <strong>完全自定义</strong> - 可以添加任何CSS样式和动画效果</li>
				<li>🎵 <strong>音效支持</strong> - 使用Web Audio API或音频文件</li>
				<li>🖼️ <strong>背景图片</strong> - 支持全屏背景、视差滚动等效果</li>
				<li>⚡ <strong>高性能</strong> - Astro的静态生成确保极快的加载速度</li>
				<li>📱 <strong>响应式设计</strong> - 完美适配各种设备</li>
				<li>🔧 <strong>易于扩展</strong> - 可以集成React、Vue等框架</li>
			</ul>

			<h2>🎯 快速开始</h2>
			<ul>
				<li>查看 <a href="/demo">功能演示页面</a> - 看看背景、音效和动画效果</li>
				<li>编辑 <code>src/pages/index.astro</code> 来修改首页</li>
				<li>在 <code>src/content/blog/</code> 中添加你的博客文章</li>
				<li>修改 <code>src/components/</code> 中的组件来自定义外观</li>
				<li>在 <code>public/</code> 目录放置图片、音频等静态资源</li>
			</ul>

			<h2>📚 如何添加功能</h2>
			<h3>添加背景图片：</h3>
			<pre><code>/* 在任何 .astro 文件的 &lt;style&gt; 标签中 */
.hero {
  background-image: url('/your-image.jpg');
  background-size: cover;
  background-position: center;
}</code></pre>

			<h3>添加音效：</h3>
			<pre><code>// 在 &lt;script&gt; 标签中
const audio = new Audio('/sounds/click.mp3');
audio.play();</code></pre>

			<h3>添加动画：</h3>
			<pre><code>/* CSS 动画 */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
.element {
  animation: fadeIn 1s ease-in;
}</code></pre>

			<h2>🛠️ 本地开发</h2>
			<pre><code>npm run dev          # 启动开发服务器
npm run build        # 构建生产版本
npm run preview      # 预览构建结果</code></pre>

			<p>
				<strong>提示：</strong>所有修改都会自动部署到 GitHub Pages！
				只需提交代码到 main 分支，GitHub Actions 会自动构建和部署。
			</p>
		</main>
		<Footer />
	</body>
</html>
